const first = 2;
const second = 2;

if(first == second){
    console.log(`${first} ⇒ ⇒ ${typeof first}  == ${second} ⇨ ⇨ ${typeof second}`,'values are equal');
}else{
    console.log(`${first} ⇒ ⇒ ${typeof first}  == ${second} ⇨ ⇨ ${typeof second}`, 'value are not equal')
}
if(first === second){
    console.log(`${first} ⇒ ⇒ ${typeof first}  === ${second} ⇨ ⇨ ${typeof second}`,'values are equal');
}else{
    console.log(`${first} ⇒ ⇒ ${typeof first}  === ${second} ⇨ ⇨ ${typeof second}`, 'value are not equal')
}

